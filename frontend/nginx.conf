server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Faz o React Router funcionar: qualquer rota (/cozinha, /admin)
        # cai no index.html em vez de retornar 404.
        try_files $uri $uri/ /index.html;
    }
}